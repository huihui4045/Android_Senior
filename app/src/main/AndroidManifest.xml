<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huihui.senior">

    <application
        android:name=".base.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".base.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".event.EventActivity" />
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".funcation.draw.UIDrawActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".funcation.paint.ui.PaintDemoActivity" />
        <activity android:name=".base.ShowActivity" />
        <activity android:name=".base.DemoActivity" />
        <activity android:name=".funcation.path.PathDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".funcation.animator.AnimatorActivity"></activity>
        <activity android:name=".base.DemoActivity" />
        <activity android:name=".funcation.material.MaterialActivity"></activity>
    </application>

</manifest>